"""ExoFlow — multi-agent software engineering orchestration.

The package contains a local AgentTeams-compatible control plane, typed
artifacts/evidence, and a runnable incident-fix pipeline built on a
deterministic, CAS-guarded state machine.
"""

__version__ = "0.1.0"
